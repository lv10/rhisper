#!/bin/sh
# $1 == 0 means this is the final removal (not an upgrade) - only then
# should the manually-created rhispertoold symlink be removed.
if [ "$1" = "0" ]; then
    rm -f /usr/bin/rhispertoold
fi

exit 0
